// Minimal helper (no dependencies). Replace with real Bootstrap JS if needed.
document.addEventListener("click", (e)=>{
  const t = e.target;
  if (t.matches("[data-nav]")) {
    e.preventDefault();
    window.location.href = t.getAttribute("href");
  }
});
